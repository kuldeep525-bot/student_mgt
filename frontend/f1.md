frontend-notes.md

2️⃣ Save this summary (this is the “memory”)
📌 PROJECT DECISIONS LOCKED

Tech (v1)

HTML, CSS, Vanilla JS

No React yet (React in v2)

Theme

Colors: Black, White, Green, Orange

Font: Inter (Google Fonts)

Centralized CSS variables in style.css

Frontend Structure

index.html → auth redirect

pages/ → login, register, dashboard

css/style.css → global theme

js/ → auth.js, dashboard.js, utils.js

Flow

Register → Login → Dashboard


UI Strategy (VERY IMPORTANT)

UI first, backend later

Dribbble-inspired design

Mix & match:

Login/Register → clean auth UI

Dashboard → modern admin panel

Notes → table-based management UI

Unified by same colors, font, spacing

Current Focus

Finish UI designs fully

Make pages look professional

No backend connection yet

“We are building a student management frontend.
UI-first approach, HTML/CSS/JS, Inter font, green/orange theme.
We already finalized file structure and basic pages.”